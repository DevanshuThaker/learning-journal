// Day 1 - Variables & Data Types

let orderCount = 5;              // number
const restaurantName = "Spice Villa"; // string
let isVeg = true;                // boolean
let discount = null;             // null
let deliveryTime;                // undefined

const menuItems = ["Paneer Tikka", "Naan", "Dal Makhani"]; // array

const cart = {
  restaurantName: "Spice Villa",
  totalAmount: 450,
  isVeg: true
}; // object

console.log(orderCount, restaurantName, isVeg, discount, deliveryTime);
console.log(menuItems);
console.log(cart);
